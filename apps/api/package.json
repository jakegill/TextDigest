{
  "name": "@td/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn src.main:app --reload --reload-dir src --port 8080",
    "build": "uv sync --frozen",
    "typecheck": "uv run mypy src",
    "lint": "uv run ruff check src"
  }
}
